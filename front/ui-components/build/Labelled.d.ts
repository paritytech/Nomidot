import React from 'react';
declare type LabelledProps = {
    children: React.ReactNode;
    isHidden?: boolean;
    label?: React.ReactNode;
    withLabel?: boolean;
};
export declare function Labelled(props: LabelledProps): React.ReactElement | null;
export {};
