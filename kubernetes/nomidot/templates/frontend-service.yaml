apiVersion: v1
kind: Service
metadata:
  name: {{ include "nomidot.frontend.fullname" . }}
  labels:
    {{- include "nomidot.frontend.labels" . | nindent 4 }}
spec:
  type: {{ .Values.frontend.service.type }}
  ports:
    - port: {{ .Values.frontend.service.port }}
      targetPort: http
      protocol: TCP
      name: http
  selector:
    {{- include "nomidot.selectorLabels" . | nindent 4 }}
